specVersion: "v1"
serviceId: "functions"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "functions-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
